{
  "name": "claude-code-execute",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "tsx index.ts",
    "build": "bun build index.ts --outdir=dist --target=node --banner=\"#!/usr/bin/env node\"",
    "test": "bun test",
    "fmt": "prettier -w ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.6.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "es-main": "^1.3.0",
    "yes-claude": "^1.2.0"
  }
}